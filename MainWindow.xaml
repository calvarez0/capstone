<Window x:Class="WPF_GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_GUI"
        mc:Ignorable="d"
        Title="BRAY S7X Configuration Software" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#2c3e50" BorderBrush="#1a252f" BorderThickness="0,0,1,0">
            <StackPanel>
                <!-- Header -->
                <Border Background="#1a252f" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="BRAY" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                        <TextBlock Text="INTERNATIONAL" FontSize="11" Foreground="#95a5a6" HorizontalAlignment="Center" Margin="0,2,0,5"/>
                        <TextBlock Text="S7X Actuator Configuration" FontSize="11" Foreground="#7f8c8d"
                                  TextWrapping="Wrap" TextAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Navigation -->
                <ListBox Grid.Column="0" SelectionMode="Single" SelectionChanged="sidebar_SelectionChanged"
                        x:Name="sidebar" BorderThickness="0" Background="Transparent">
                    <local:NavButton Navlink="/Pages/ConnectionPage.xaml" Margin="5" Padding="12" Label="Connection"/>
                    <local:NavButton Navlink="/Pages/ControlPage.xaml" Margin="5" Padding="12" Label="Control"/>
                    <local:NavButton Navlink="/Pages/ConfigPage.xaml" Margin="5" Padding="12" Label="Configuration"/>
                    <local:NavButton Navlink="/Pages/NetworkPage.xaml" Margin="5" Padding="12" Label="Network"/>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Frame Grid.Column="1" x:Name="navframe" NavigationUIVisibility="Hidden" Background="White"/>
    </Grid>
</Window>